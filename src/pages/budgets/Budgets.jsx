const Budgets = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold text-gray-800 mb-6">Ngân sách (Budgets)</h1>
      <div className="bg-white p-6 rounded-lg shadow-sm border border-gray-100">
        <p className="text-gray-500 italic">Thiết lập target chi tiêu. Component này cần dựng các progress bars (Ví dụ: `(total_spent / target_amount) * 100`) theo guide.</p>
      </div>
    </div>
  );
};

export default Budgets;
